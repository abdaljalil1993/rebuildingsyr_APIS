# نظام طلبات المساعدة والتبرعات - دليل الواجهات البرمجية

## مقدمة النظام

تم تطوير نظام شامل لإدارة طلبات المساعدة والتبرعات يسمح للمستخدمين العاديين بتقديم طلبات مساعدة متنوعة (تبرعات مالية، تطوع، ملابس، إلخ) ويوفر واجهات برمجية متخصصة للبحث عن هذه الطلبات والتواصل مع مقدميها.

---

## 1. المسارات (Routes) الرئيسية

### أ) المسارات العامة (Public Routes) - بدون مصادقة
```
GET    /requests/public             - عرض قائمة طلبات المساعدة المعتمدة
GET    /requests/public/:id         - عرض تفاصيل طلب مساعدة معين
```

### ب) المسارات الخاصة بالمستخدم (Authenticated Routes) - مع مصادقة
```
POST   /requests                    - إنشاء طلب مساعدة جديد
GET    /requests/my                 - عرض طلبات المستخدم
GET    /requests/:id                - عرض تفاصيل طلب المستخدم
PATCH  /requests/:id                - تحديث طلب المستخدم
DELETE /requests/:id                - حذف طلب المستخدم
```

---

## 2. الواجهات العامة (Public APIs)

### 2.1 عرض قائمة طلبات المساعدة المعتمدة
**المسار:** `GET /requests/public`

**الوصف:** عرض جميع طلبات المساعدة المعتمدة من قبل الإداريين مع معلومات المستخدم ووسائل التواصل.

**معاملات الاستعلام (Query Parameters):**
```
- page         (اختياري): رقم الصفحة (الافتراضي: 1)
- limit        (اختياري): عدد النتائج لكل صفحة (الافتراضي: 10)
- serviceId    (اختياري): تصفية حسب نوع الخدمة
- city         (اختياري): تصفية حسب المدينة
- search       (اختياري): البحث حسب اسم المستخدم أو نوع الخدمة
- sortBy       (اختياري): ترتيب النتائج (newest, oldest)
```

**مثال على الطلب:**
```
GET /requests/public?page=1&limit=10&city=دمشق&serviceId=2
```

**مثال على الاستجابة الناجحة:**
```json
{
  "success": true,
  "data": [
    {
      "id": 1,
      "status": "APPROVED",
      "createdAt": "2024-01-15T10:30:00Z",
      "updatedAt": "2024-01-16T14:45:00Z",
      "service": {
        "id": 2,
        "name": "تبرع مالي",
        "description": "تبرع لمساعدة الأسر المحتاجة"
      },
      "user": {
        "id": 5,
        "name": "أحمد محمد",
        "phone": "+963999999999",
        "email": "ahmad@example.com",
        "city": "دمشق"
      },
      "data": [
        {
          "id": 1,
          "requestId": 1,
          "fieldId": 10,
          "value": "5000 ليرة سورية",
          "field": {
            "id": 10,
            "name": "المبلغ",
            "type": "number"
          }
        }
      ],
      "media": [
        {
          "id": 1,
          "requestId": 1,
          "filePath": "/uploads/proof.jpg",
          "type": "image"
        }
      ]
    }
  ],
  "meta": {
    "page": 1,
    "limit": 10,
    "total": 45,
    "totalPages": 5
  }
}
```

---

### 2.2 عرض تفاصيل طلب مساعدة معين
**المسار:** `GET /requests/public/:id`

**الوصف:** عرض تفاصيل كاملة لطلب مساعدة معين بما يتضمن معلومات المستخدم ووسائل التواصل والملفات المرفقة.

**معاملات المسار (Path Parameters):**
- `id` (مطلوب): رقم الطلب

**مثال على الطلب:**
```
GET /requests/public/1
```

**مثال على الاستجابة الناجحة:**
```json
{
  "success": true,
  "data": {
    "id": 1,
    "status": "APPROVED",
    "createdAt": "2024-01-15T10:30:00Z",
    "updatedAt": "2024-01-16T14:45:00Z",
    "service": {
      "id": 2,
      "name": "تبرع مالي",
      "description": "تبرع لمساعدة الأسر المحتاجة"
    },
    "user": {
      "id": 5,
      "name": "أحمد محمد",
      "phone": "+963999999999",
      "email": "ahmad@example.com",
      "city": "دمشق"
    },
    "data": [
      {
        "id": 1,
        "requestId": 1,
        "fieldId": 10,
        "value": "5000 ليرة سورية",
        "field": {
          "id": 10,
          "name": "المبلغ",
          "type": "number",
          "required": true
        }
      },
      {
        "id": 2,
        "requestId": 1,
        "fieldId": 11,
        "value": "مساعدة لأسرة مكونة من 5 أفراد",
        "field": {
          "id": 11,
          "name": "تفاصيل الحالة",
          "type": "text",
          "required": true
        }
      }
    ],
    "media": [
      {
        "id": 1,
        "requestId": 1,
        "filePath": "/uploads/proof.jpg",
        "type": "image",
        "createdAt": "2024-01-15T10:35:00Z"
      },
      {
        "id": 2,
        "requestId": 1,
        "filePath": "/uploads/document.pdf",
        "type": "pdf",
        "createdAt": "2024-01-15T10:36:00Z"
      }
    ]
  }
}
```

**أكواد الخطأ المحتملة:**
- `404`: الطلب غير موجود
- `403`: الطلب موجود لكن لم يتم الموافقة عليه بعد

---

## 3. الواجهات الخاصة بالمستخدم المسجل (Authenticated APIs)

### 3.1 إنشاء طلب مساعدة جديد
**المسار:** `POST /requests`

**الوصف:** السماح للمستخدم بتقديم طلب مساعدة جديد مع البيانات والملفات المطلوبة.

**المصادقة:** مطلوبة (Authentication Required)

**البيانات المطلوبة:**
```json
{
  "serviceId": 2,
  "data": [
    {
      "fieldId": 10,
      "value": "5000 ليرة سورية"
    },
    {
      "fieldId": 11,
      "value": "مساعدة لأسرة مكونة من 5 أفراد"
    }
  ],
  "media": [
    {
      "filePath": "/uploads/proof.jpg",
      "type": "image"
    },
    {
      "filePath": "/uploads/document.pdf",
      "type": "pdf"
    }
  ]
}
```

**مثال على الاستجابة الناجحة:**
```json
{
  "success": true,
  "message": "تم إنشاء الطلب بنجاح",
  "data": {
    "id": 1,
    "userId": 5,
    "serviceId": 2,
    "status": "PENDING",
    "createdAt": "2024-01-15T10:30:00Z",
    "updatedAt": "2024-01-15T10:30:00Z",
    "rejectionReason": null,
    "user": {...},
    "service": {...},
    "data": [...],
    "media": [...],
    "notes": []
  }
}
```

---

### 3.2 عرض طلبات المستخدم
**المسار:** `GET /requests/my`

**الوصف:** عرض جميع طلبات المستخدم الحالي.

**المصادقة:** مطلوبة

**معاملات الاستعلام:**
```
- page         (اختياري): رقم الصفحة
- limit        (اختياري): عدد النتائج لكل صفحة
- status       (اختياري): تصفية حسب الحالة (PENDING, APPROVED, REJECTED, UNDER_REVIEW, NEEDS_INFO)
- serviceId    (اختياري): تصفية حسب نوع الخدمة
- search       (اختياري): البحث حسب كلمات مفتاحية
```

---

### 3.3 عرض تفاصيل طلب المستخدم
**المسار:** `GET /requests/:id`

**الوصف:** عرض تفاصيل طلب معين للمستخدم الحالي فقط.

**المصادقة:** مطلوبة

---

### 3.4 تحديث طلب المستخدم
**المسار:** `PATCH /requests/:id`

**الوصف:** تحديث بيانات طلب المستخدم (متاح فقط عندما تكون حالة الطلب PENDING أو NEEDS_INFO).

**المصادقة:** مطلوبة

**البيانات المطلوبة:**
```json
{
  "data": [
    {
      "fieldId": 10,
      "value": "القيمة الجديدة"
    }
  ]
}
```

---

### 3.5 حذف طلب المستخدم
**المسار:** `DELETE /requests/:id`

**الوصف:** حذف طلب المستخدم (لا يمكن حذف الطلبات المعتمدة).

**المصادقة:** مطلوبة

---

## 4. حالات الطلب (Request Status)

يمكن للطلب أن يمر بالحالات التالية:

| الحالة | الوصف |
|-------|-------|
| `PENDING` | الطلب قيد الانتظار ولم تتم مراجعته بعد |
| `UNDER_REVIEW` | الطلب تحت المراجعة من قبل المراجعين |
| `NEEDS_INFO` | الطلب يحتاج معلومات إضافية من المستخدم |
| `APPROVED` | تمت الموافقة على الطلب ويظهر للعامة |
| `REJECTED` | تم رفض الطلب |

---

## 5. أنواع الخدمات (Service Types)

يمكن تحديد أنواع الخدمات التالية:

| النوع | الوصف |
|-------|-------|
| تبرع مالي | تقديم مبلغ مالي |
| تطوع | المساهمة بالعمل والوقت |
| ملابس | التبرع بملابس |
| غذاء | التبرع بمواد غذائية |
| تعليم | المساعدة في التعليم |
| صحة | المساعدة الصحية |

---

## 6. معايير الأمان والخصوصية

### 6.1 المعلومات المعروضة للعامة
عند عرض طلب مساعدة معتمد للعامة، يتم عرض المعلومات التالية فقط:
- اسم المستخدم
- رقم الهاتف
- البريد الإلكتروني
- المدينة
- تفاصيل نوع المساعدة المطلوبة
- الملفات المرفقة

### 6.2 تصفية الطلبات
- الطلبات غير المعتمدة لا تظهر في البحث العام
- يمكن للمستخدم عرض طلباته الخاصة فقط
- المراجعون والإداريون لديهم صلاحيات إضافية

---

## 7. أمثلة عملية للاستخدام

### 7.1 تقديم طلب تبرع مالي

**الخطوة 1:** الحصول على التوكن (Token) من الدخول
```bash
POST /auth/login
{
  "email": "user@example.com",
  "password": "password123"
}
```

**الخطوة 2:** إرسال الطلب
```bash
POST /requests
Headers: {
  "Authorization": "Bearer YOUR_TOKEN"
}

Body: {
  "serviceId": 2,
  "data": [
    {"fieldId": 10, "value": "10000 ليرة"},
    {"fieldId": 11, "value": "عائلة محتاجة"}
  ],
  "media": [
    {
      "filePath": "/uploads/family-photo.jpg",
      "type": "image"
    }
  ]
}
```

### 7.2 البحث عن طلبات في المدينة

```bash
GET /requests/public?city=دمشق&serviceId=2&page=1&limit=10
```

### 7.3 الحصول على تفاصيل طلب معين

```bash
GET /requests/public/1
```

---

## 8. رسائل الخطأ الشائعة

| الخطأ | السبب | الحل |
|------|------|------|
| 404 Not Found | الطلب غير موجود | تحقق من رقم الطلب |
| 403 Forbidden | ليس لديك صلاحية | تأكد من المصادقة أو الصلاحيات |
| 400 Bad Request | البيانات غير صحيحة | تحقق من صيغة البيانات |
| 401 Unauthorized | مصادقة مطلوبة | سجل الدخول أولاً |

---

## 9. استراتيجيات التطوير المستقبلية

### إضافات مخطط لها:
1. **نظام التقييم والملاحظات**: السماح للمتبرعين بتقييم الطلبات
2. **إشعارات فورية**: إخطار المستخدمين عند إجراء تحديثات على طلباتهم
3. **خيارات البحث المتقدم**: البحث بحسب المسافة الجغرافية والمدة الزمنية
4. **نظام المراسلة المباشرة**: التواصل المباشر بين المتبرعين ومقدمي الطلبات
5. **التقارير والإحصائيات**: عرض إحصائيات الطلبات والمساعدات المقدمة

---

## 10. ملاحظات تقنية مهمة

- جميع التواريخ بصيغة ISO 8601 (UTC)
- جميع الطلبات المحمية بمفتاح JWT
- الملفات يجب أن تكون في مجلد `/uploads`
- الحد الأقصى لحجم الملف: 5MB
- الصيغ المدعومة: JPG, PNG, PDF

---

**آخر تحديث:** 2024-01-15
**الإصدار:** 1.0
